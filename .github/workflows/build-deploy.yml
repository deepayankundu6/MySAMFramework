name: Build and Delpoy app
run-name: ${{ github.actor }}
on:
  push:
    branches: [main]

jobs:
  workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: "18.x"
      - uses: build.yml
      - uses: deploy.yml
